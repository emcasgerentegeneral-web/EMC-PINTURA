services:
  - type: web
    name: emc-pintura-web
    runtime: static
    buildCommand: echo "Sitio público listo"
    staticPublishPath: ./public
    headers:
      - path: /*
        name: X-Content-Type-Options
        value: nosniff
      - path: /*
        name: Referrer-Policy
        value: strict-origin-when-cross-origin
